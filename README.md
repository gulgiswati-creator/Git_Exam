# Git_Exam
This repo is for git lab exam which have exp5 called collabration and remote repositories
##fetch the latest changes from remote repositry and rebase your local branch onto updated branch
<p>
steps for above experiments are :
git checkout <branch-name>
git fetch origin
git rebase origin <branch-name>
git push origin <branch-name>
</p>
